;; A polymorphic function instantiated at a function type.
;; ==
;; tags { higher-order }
;; output { 1 }
(entry (main)
(let (
  (t-fun (id (&t)) (fn ((x &t)) x))
)
  (((t-app id (-> Int Int)) (t-app id Int)) 1)
)
)
